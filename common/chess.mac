/*** chess macros ***/

#define RANK_OF(pos) ((pos) / NUM_FILES)
#define FILE_OF(pos) ((pos) % NUM_FILES)

#define POS_OF(rank,file) ((rank) * NUM_FILES + file)

#define ABS_VAL(a,b) (((a) > (b)) ? (a - b) : (b - a))
